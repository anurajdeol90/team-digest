name: Weekly Digest (schedule)

on:
  schedule:
    # Mondays 9:00 America/Chicago
    - cron: "0 14 * * 1"
  workflow_dispatch: {}

jobs:
  call-publish:
    uses: ./.github/workflows/publish-digests.yml
    with:
      digest: weekly
      date: ""
      dry_run: false
